Sauna project

    NodeMCU esp8622(?)
    - read current temp
    - send it to server

    Server (express)
    - save temps to MongoDB
    - read temps from MongoDB
    - docker

    UI (Vite + React)
    - display current temp
    - display latest temps on chart


Secrets for WiFi access were moved to Arduino library
new .h file has to be created in the library folder with defined secret values

